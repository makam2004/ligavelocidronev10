// Estos valores NO se usan directamente en el navegador para credenciales sensibles.
// El frontend sólo hace peticiones relativas al mismo host (/api/...).
// Deja placeholders por si quieres leer Supabase desde frontend en el futuro.
window.SUPABASE_URL = window.SUPABASE_URL || '';
window.SUPABASE_ANON_KEY = window.SUPABASE_ANON_KEY || '';
